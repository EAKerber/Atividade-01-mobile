<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projetoatividade01">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoAtividade01">
        <activity android:name=".LayoutRecyclerReview"></activity>
        <activity android:name=".adapter.RecycleReview" />
        <activity android:name=".DetalheTodoActivity" />
        <activity android:name=".PaginaInicial" />
        <activity android:name=".TelaLogin" />
        <activity android:name=".SpashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>